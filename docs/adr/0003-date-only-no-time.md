# 紀錄日期只到「日」，不存時間與時區

存款與消費的日期以 Postgres `date` 型別存放，不帶時間、不帶時區。同一天內的排序與追蹤用系統自動填入、使用者不可見的 `created_at` 時間戳。

## Considered Options

- **`timestamptz`**：使用者每筆要多填時間（或補記時要改），且台灣 +8 與 UTC 在跨午夜時會讓紀錄落到錯的一天，是跨時區日期分組最常見的 bug。

## Consequences

- 日 / 月 / 年統計純粹以日期分組，沒有任何時區轉換。
- 使用者無法記錄「幾點花的」；若未來需要，要新增欄位而非改型別。
